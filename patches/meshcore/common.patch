diff --git a/examples/companion_radio/main.cpp b/examples/companion_radio/main.cpp
--- a/examples/companion_radio/main.cpp
+++ b/examples/companion_radio/main.cpp
@@ -189,7 +189,7 @@
   #endif
     the_mesh.startInterface(serial_interface);
 #elif defined(ESP32)
-  SPIFFS.begin(true);
+  SPIFFS.begin(true, "/spiffs", 10, "core_fs");
   store.begin();
   the_mesh.begin(
     #ifdef DISPLAY_CLASS

diff --git a/src/OTAValidation.cpp b/src/OTAValidation.cpp
new file mode 100644
--- /dev/null
+++ b/src/OTAValidation.cpp
@@ -0,0 +1,5 @@
+#include <Arduino.h>
+
+extern "C" bool verifyRollbackLater() {
+    return true;
+}

diff --git a/src/helpers/ESP32Board.h b/src/helpers/ESP32Board.h
--- a/src/helpers/ESP32Board.h
+++ b/src/helpers/ESP32Board.h
@@ -23,6 +23,12 @@
 
 public:
   void begin() {
+    {
+      uint8_t baseMac[6];
+      esp_read_mac(baseMac, ESP_MAC_WIFI_STA);
+      baseMac[5] = (baseMac[5] + 1) & 0xFF;
+      esp_base_mac_addr_set(baseMac);
+    }
     // for future use, sub-classes SHOULD call this from their begin()
     startup_reason = BD_STARTUP_NORMAL;    
 
